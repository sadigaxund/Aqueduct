# Snippet 03 — Delta incremental ingress
# Delta JAR is fetched by Spark at runtime via spark.jars.packages.
# No pyspark/delta-spark pip pins here — the populate script dynamically
# selects the right Delta JAR artifact based on the installed pyspark
# version.  Pinning pyspark in this shared-venv CI run would cascade to
# all subsequent snippets.
rich>=13.0   # needed by inspect_results.py
pandas>=2.0  # needed by populate_delta.py
