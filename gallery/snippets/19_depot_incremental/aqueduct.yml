aqueduct_config: "2.0"

warnings:
  suppress:
    - "*"

# aqueduct.yml
deployment:
  engine: spark
  target: local

engine:
  spark:
    master_url: "local[*]"

stores:
  observability:
    backend: duckdb
    path: .aqueduct
  # The Depot store persists watermarks for incremental processing
  depots:
    default:
      backend: duckdb
      path: .aqueduct/depot.db
