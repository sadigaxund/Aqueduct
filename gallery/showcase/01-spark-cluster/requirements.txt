# Aqueduct engine + Spark extra (pulls pyspark==4.0.x, delta-spark, py4j).
aqueduct-core[spark]

# Pin pyspark to match the cluster image (4.0.0 — see docker-compose.yml).
# Mismatched major.minor.patch breaks RPC with serialVersionUID errors.
pyspark==4.0.0

# Used only by seed.py to upload synthetic parquet to MinIO.
boto3
pyarrow
pandas
